# RESOLVED_FACTS — 確定事実・解決済み台帳（真実源）

> 委員会(`scripts/committee.py`)が会議冒頭で読む「いま何が解決済み・設定済みか」の真実源（AUTONOMY §2「真実源」）。
> **目的**：解決済みの論点を「未着手・未設定」として蒸し返さない／古い前提を本日の新情報で更新する。
> 制定：2026-06-13。proposal-stress-test 済。

---

## 🔑 運用ルール（これを守らないと真実源が逆に事故る）

1. **解決したら、その日のうちにここへ追記する**（人＋Claude の癖）。追記しない台帳は陳腐化し、委員会が古い"確定事実"を盲信する＝より悪い事故になる。
2. **各項目に as-of 日付を必ず付ける**。日付の古い項目は「再検証の候補」。
3. **検知（コード）と解釈（プロンプト）は層を分ける**。障害の検知は `committee.py` の原則7（成果ゼロ→Slack通知）や `daily_report.py` 等の**コード**が担う＝この台帳のプロンプト判断では握りつぶせない。本台帳は「既に済んだことを蒸し返さない」ための参照であり、**新障害を隠すものではない**。
4. **本日の実データ・ログが確定事実と矛盾したら、委員会は即「確定事実の更新要」として再提起する**（例：「GA4は稼働中」とあるのに本日 401/取得失敗が出たら、済み扱いをやめて継続審議へ）。

---

## 確定事実（as-of 付）

### GA4 計測基盤 — 設定済み・稼働中（as-of 2026-06-08）
- GitHub Secrets `GA4_PROPERTY_ID` / `GOOGLE_CREDENTIALS_JSON` は **2026-06-08 に登録済み**。クラウド委員会/daily_report は **実データを取得できている**（2026-06-13 の data担当が「セッション2/ユーザー1/PV2/滞在49秒」を取得）。
- ✅ **「GA4 Secrets を登録してください」という俊雄さんへの依頼は出さない**（済み）。
- 🔧 **診断ファネルのカスタムイベント — 大半が実装済み（as-of 2026-06-19 コードで確認）**：`life-oracle-v2/src/lib/analytics.js` の `trackEvent()`（→`window.gtag('event',...)`）経由で、**`app_loaded` / `quiz_start`（mbti/situation/attack）/ `quiz_complete`（mbti_type・jin_id付）/ `result_share_intent` / `lead_captured` が既に発火している**。
  - ✅ **完走率は今すぐ算出可能**（`quiz_start`数 vs `quiz_complete`数）。「完走率が算出不可」は**誤り**＝この前提で蒸し返さない。`diagnosis_start`/`complete` は **名称違いで実体は実装済み**（`quiz_start`/`quiz_complete`）。
  - ❌ **唯一の未実装＝`question_view`（設問ごとの閲覧イベント）**。これだけが「どの設問で離脱したか（離脱設問）」の算出に必要。残るアプリ側コード作業はこの1イベントのみ。
  - ⚠️ 委員会/データ担当が「カスタムイベント未実装・完走率算出不能」を繰り返してきたのは、本台帳のこの項目が不正確だったため（2026-06-19 修正）。

### gachijin パイプライン — 引退（as-of 2026-06-13・[[NOT_DOING.md]] #8）
- gachi は2026-11月まで予約完了済み＝パイプラインは役目を終えている。`gachijin-pipeline` / `gachijin-scheduler` の古いCI失敗（直近実行2026-05-08）は**無視してよい**。修復を律速・最優先として採択しない。daily_report も `RETIRED_WORKFLOW_KEYWORDS` で報告・再実行から除外済み。

### 委員会のクラウド自動稼働 — 稼働中（as-of 2026-06-13）
- `committee-research.yml`(10:00 JST) / `committee-meeting.yml`(14:00) / `daily-report.yml`(08:00) は **main にあり定時稼働中**。「委員会のクラウド稼働準備」は完了＝未着手扱いしない。
- committee.py は計画拘束（WEEKLY_SPRINT/NOT_DOING/本台帳を読む）＋DO-NOT-ADOPT除外＋執行レビュー込み。

### 委員会の実行方式を週次ローカル無人実行へ移行（as-of 2026-07-09）
- 🟢 **`committee-research.yml`/`committee-meeting.yml`（GitHub Actions・生API課金・毎日10:00/14:00）は、2026-07-02以降のAnthropicプリペイド残高枯渇を機に、`scripts/committee_native/`（Claude Code `claude -p --permission-mode bypassPermissions` によるTaskツール5担当会議・サブスク課金枠）へ後継移行した。** ローカル週次タスク `LifeOracle_Committee_Weekly`（毎週月曜19:00 JST）が起動し、`tasks/committee/LEDGER.md` への追記・（必要なら）本台帳の訂正追記・git push・Slack通知までを無人で行う。プロンプト本体＝`scripts/committee_native/weekly_prompt.md`、ランナー＝`scripts/committee_native/run_weekly.ps1`。
- 2026-07-09に技術検証済み：`claude -p`のTaskツールは実ファイル読取を伴うサブエージェント起動に成功（12.5秒・permission_denials 0件）。同日、この方式で試験的に会議1回を実施しLEDGER.mdへ反映・push済み（コミット40bc4dc）。
- ✅ **2026-07-13、GitHub Actions側 `committee-research.yml`/`committee-meeting.yml` は俊雄さん判断で無効化済み**（`gh workflow disable` 290589919/290589920・毎日の空振り失敗メール(8:45×3+13:23等)が煩わしいため）。**削除ではなく無効化のみ＝`gh workflow enable` でいつでも復元可能。** 以後、無人で毎日届いていた失敗通知メールは来なくなる。「委員会が動いていない」という報告が今後来た場合、まずこのローカル週次タスク（`LifeOracle_Committee_Weekly`）とLEDGER.mdの最新日付を確認すること。
- 毎週の頻度に決定した理由：2026-06-20〜07-09の19日間、日次自動運用の停止が誰にも気づかれなかった実績、および1回の会議で決定事項が10件超発生し日次では消化しきれない実態から、俊雄さんの判断で週次に変更。
- 🔧 **【訂正・2026-07-13追記】週次無人実行（`LifeOracle_Committee_Weekly`・毎週月曜19:00）は本日、日本語パス破損・引数切り詰めが原因で3回連続失敗した（`scripts/committee_native/logs/` 21:21〜21:28の3件・うち1件はプロンプト欠落を検知した人力デバッグ応答）。修正コミット9a06ac1（21:31・「日本語パス破損・引数切り詰めを修正」）を経て、同日中に手動再実行した回で初めて6担当（配信担当新設込み）の会議が正常完走した。∴ **「週次ローカル無人実行への移行は完了・安定稼働」という上記の記載は一部訂正が必要**：移行自体は完了しているが、日本語パスを含む環境での引数受け渡しに脆弱性があり、今後も同種の文字化け/切り詰め障害が再発しうる。次回月曜19:00の自動起動が今回の修正で安定するかは未検証（要確認）。詳細＝`tasks/committee/LEDGER.md` 2026-07-13節。
  - ✅ **【追記】同日21:31の修正後、`Start-ScheduledTask`で実タスクを直接再起動し検証（Task Schedulerと同一のOS機構・21:31起動）＝602秒・21ターン・$14.69相当で6担当会議が正常完走、LEDGER追記＋push（commit 4c09deb）まで無人完走を確認済み。** 3回の失敗原因はいずれも「配信担当セクション追加でプロンプトが4877文字に伸び、位置引数渡しの経路のどこかで切り詰められた」ことに起因（日本語パスのBOM問題は独立した別バグとして先に修正済み）。対処＝配信担当の説明を4156文字に簡潔化。次回月曜19:00の完全無人自動起動（人手を介さない）はまだ未検証＝要確認だが、起動経路自体は同一のため安定稼働の見込みは高い。

### Anthropic API キー — GitHub Secret 同期済み（as-of 2026-06-13）
- 2026-06-11〜13 に 401(invalid x-api-key) で委員会が4日停止 → ローカルの有効キーを `ANTHROPIC_API_KEY` Secret に同期して復旧。
- ⚠️ **キーをローテしたら GitHub Secret も `gh secret set ANTHROPIC_API_KEY` で必ず更新**（ローカル .env だけだとクラウドが401で止まる）。

### stress_test baseline — 記録済み（as-of 2026-06-14）
- `life-oracle-v2/scripts/stress_test.mjs` を実行し `scripts/baseline.json` に保存：**dist gzip 291.7KB / build 9.0秒 / 診断計算 0.22ms（中央値）**。
- ✅ **「stress_test baseline を記録せよ」の3回連続持ち越しは解消**。委員会は再提案しない（次は回帰検知＝baselineとの比較に進む）。

### GSC（Google Search Console）連携 — 連携済み・稼働中（as-of 2026-06-14）
- **サービスアカウント方式で連携完了**。SA `life-oracle-ga4@gen-lang-client-0791471309.iam.gserviceaccount.com` を Search Console の `https://life-oracle.jp/` に siteFullUser で追加済み＋GCPで Search Console API 有効化済み。SA実APIで疎通確認済み。
- 委員会 SEO担当は `get_gsc_stats()`（`daily_report.py`）経由で**GSC実データ（直近28日のクリック/表示/上位クエリ）を取得**。クラウドは `GOOGLE_CREDENTIALS_JSON`(同一SA)、ローカルは `monitoring/config/ga4-sa.json`(発行したSA鍵)。
- ⚠️ OAuth(gcloud ADC)方式は Google にブロックされる（汎用クライアント＋機微スコープ）ため**SA方式が正**。`adc.json`(ユーザーADC)はGA4専用(analyticsのみ)。
- ✅ **「GSC連携を」「所有権確認を」の再依頼は出さない（済み）。** 連携は機能。残課題は「検索に載る」だが、その真因は下記（コンテンツでなく重複）。

### GA4/GSC 徹底調査 — アプリSEO不振の真因特定＋計測実態（as-of 2026-06-19・実API確認）
- 🔴 **アプリが検索に載らない真因＝Netlify生サブドメインが正規URLを奪っていた（修正済）**：GSC URL検査で `googleCanonical=https://incredible-llama-51caa2.netlify.app/`＝life-oracle.jpは重複扱い。`life-oracle-v2/netlify.toml` にホスト301（netlify.app/*→life-oracle.jp）を追加・デプロイ済（curlで301確認）。**SEO不振はコンテンツ不足でなくこの重複が真因**＝「記事を増やせ/タイトルだけ直せ」で蒸し返さない。次＝Google再クロール＋GSCインデックス登録リクエスト（俊雄さん操作）。
- 🔴 **note記事のインデックスはGSCで不可視**：note.com＝別ドメイン・所有外。委員会の「GSCでnote記事を点検」は前提誤り＝再提案しない（site:検索のみ・US-IPでは不発）。
- 🔧 **GA4ローカル読み取りは権限不足**：ローカルSAはGA4プロパティのViewer権限ゼロ（GSCには有）。`monitoring/config/sites.json`の life-oracle.jp=`534433669` はSAから読めず＝誤りor権限未付与。GA4 Admin APIはGCPで無効でプロパティ自動特定不可。**クラウド日次は取得成功**。→ 俊雄さん操作待ち（SAをpropertyにViewer追加／Admin API有効化 or `GA4_PROPERTY_ID`値開示）。済むまでローカルGA4は乖離。
  - 🔧 **【訂正・2026-07-09追記】上記はサービスアカウント方式（`monitoring/config/ga4-sa.json`）に限った話。OAuth ADC方式（`monitoring/config/adc.json`）では別ルートとして権限が生きており、2026-07-09にデータ担当がこの方式でGA4へ直接クエリし、直近28日の実データ（app_loaded/quiz_start/quiz_complete/question_view等）取得に成功した。** ＝ローカルでGA4を見る手段はSA方式が塞がっていても既に存在する（ADC経由）。以後「ローカルGA4取得不可」を理由に俊雄さんへ再依頼を出さない。SA方式の権限解放は依然として保留（優先度は下がる）。
- ✅ **question_view 実装済（2026-06-19デプロイ）**：離脱設問も算出可能に（完走率は元から quiz_start/complete で算出可）。詳細＝`tasks/committee/ga4_gsc_investigation_2026-06-19.md`。

### GA4 海外（US）流入の正体 — 非エンゲージのノイズ＝英語化の根拠にならない（as-of 2026-06-21・GA4実UI確認）
- 🔴 **「異常値メール＝海外流入」を英語化の判断材料にしない（否）**。GA4「ユーザー属性の詳細：国」（過去28日 5/24-6/20）の実数：**US＝15ユーザー（最多）だがエンゲージのあったセッションは1件のみ（全体の5.88%）・エンゲージメント率6.67%・1ユーザーあたりエンゲージ0.07**。対して **Japan＝エンゲージ15件（全体の88.24%）・エンゲージ率68.18%**。∴ **28日で診断に実際に触れたセッションの88%は日本、海外(US)は1件だけ**＝海外流入は実質ほぼ非エンゲージ（直帰/ボット/クローラ/Netlify自動アクセス＝先日301した生netlify.app含む）。異常値メールは母数極小ゆえ非エンゲージのスパイクをGA4が異常検知しただけ＝実需ではない。
- ⚠️ US平均エンゲージ時間1分52秒は**エンゲージ率6.67%と矛盾するn=1小標本アーティファクト**＝「海外の人間が熱心」と読むのは誤読。信頼すべきは率(6.67%)と件数(1)。
- ✅ **英語化の再検討トリガー＝海外の「エンゲージのあったセッション」が複数月で持続した時のみ（目安：月30件+・エンゲージ率40%+）。** 閾値はユーザー数(膨らみやすい)でなく**エンゲージ件数**。現状は28日でUSエンゲージ1件＝トリガー未達。委員会は「海外流入→英語化」で蒸し返さない。
- 🟢 **参照元レベルの裏付け（2026-06-21・GA4「トラフィック獲得」参照元/メディア・28日）＝note翻訳由来の海外読者説は否定／noteファネルは機能**：本物の人間流入は**全てnote由来**＝`note/article`(10セッション・エンゲージ率50%)＋`note.com/referral`(4セッション・**エンゲージ率100%・平均4分21秒**)で**全エンゲージ17件中9件(53%)を供給**＝記事CTA→アプリ導線は効いている（＝「届け方＝note」は正しく回っている・俊雄さんの「認知はnoteのみ」認識が正）。一方 海外(US)の正体は**note非経由**＝`(not set)`4セッションが**0エンゲージなのに724イベント/セッション(≒2,900イベント)** のJS実行型ボット＋`(direct)/(none)`の非エンゲージ分（米data center IP）。∴ もしnote翻訳で海外読者が流入なら`note/article`がUS地理＋エンゲージで出るはずだが、note由来は全てエンゲージ(=日本)。**海外流入=ボット確定・英語化根拠でない**。副次：`(not set)`724イベント/セッションは異常値＝GA4ボット除外設定 or question_viewの外部クローラ暴発を一度点検する価値（ただし実ユーザーのバグではない・(not set)4セッションに限局）。
- 🔧 **GA4でのボット除外は仕様上不可＝アプリ側guardで源流抑止済み（2026-06-21・実機確認＋デプロイ）**：GA4データフィルタは「デベロッパートラフィック/内部トラフィック（どちらもIP指定）」の2種のみ＝**国別/参照元別/(not set)別の除外は存在しない**（既知ボットの自動除外は標準ON・OFF不可）。ボットIPはレポートに出ずブラインドIP除外は実ユーザー誤除外リスクゆえ不採用。→ 代わりに `life-oracle-v2/src/lib/analytics.js` の `trackEvent()` に `navigator.webdriver` ＋ bot/headless UA署名のガードを追加しデプロイ（commit 73a403b）。ヘッドレス型（今回の(not set)724イベント型）を源流で抑止・**実ユーザーは webdriver=false で無影響**・IP偽装型は部分的。∴ **委員会は「GA4でボット除外設定を」を再提案しない（GA4では不可・アプリ側で対処済み）**。ホームの異常検知バッジ（216.7%⚠️）等の自動インサイトはGA4仕様でOFF不可＝設定では消せない（実トラフィック増で自然に埋もれるのを待つ）。
  - 🔴 **【訂正・2026-07-09追記】「抑止済み」は部分的だったと実データで判明**：6/21ガード後の**6/24に別シグネチャのマルチブラウザ・スパイクが再発**（データ担当が直近28日app_loadedをGA4で実クエリ：1,208件中1,162件＝96%が6/18・6/19・6/24の3日に集中。6/24型はソースが`(not set)`→`(direct)`に変化し複数ブラウザを横断、GA4の「エンゲージ」判定を通過する＝webdriverガードをすり抜けている）。∴ **IP偽装型・非webdriver型のボットには引き続き無防備**であり、「アプリ側guardで源流抑止済み」を全面解決と扱わない。次の一手＝発生源（外部ボット／社内QA実機テストの誤計測）の切り分け（本日エンジン担当への申し送り）。集計時は6/18・6/19・6/24を除外した数値を「地に足のついた」実測として扱うのが当面の運用（データ担当7/9レポート）。

### 神プロンプト配布施策ローンチ＋X投稿リンク修正＋API上限（as-of 2026-06-26）
- 🟢 **「あなたを"神"にする」プロンプト機能＝結果画面に実装済・本番ライブ**（`life-oracle-v2/src/pages_v2/MbtiResult.jsx` の `buildDeityPrompt`／commit cbf4f83・c232add・528e75e）。診断結果（年代×職業×タイプ×バイアス×処方箋）からChatGPT用プロンプトを生成→「自分を模した神」画像を作ってシェア＝配布力強化施策。あわせてシェアボタンを`navigator.share`化・PWA「ホーム画面に追加」プロンプト追加・Entry/MbtiEntry導線整備。note記事（ChatGPTで自分を神にする無料プロンプト配布）公開＋トップ固定済（`note.com/lifeoraclejp/n/n280b925275c8`）。＝**現在の配布の主施策**。委員会は「シェア機能/配布手段を作れ」を実装済として扱い再提案しない。詳細＝[[memory: project_deity_prompt_distribution]]。
- 🔧 **X自動投稿のリンク欠落＝多層故障を全修正し単発のみへ確定（2026-06-26）**：真因＝①スレッド型はPlaywright返信連投が壊れ1ツイート目しか出ない（孤立・リンク無し）②Geminiが180字を守れず単発生成も失敗③過去投稿は本文URL混在でリーチ抑制。対処＝スレッド完全廃止（`ENABLE_THREAD_TYPES=False`）・単発上限を180→280字に緩和（青バッジ＝長文可）・`poster.py`の`finalize_tweet_text`で本文末尾にnote URLを構造付与＋投稿直前`_url_guard_or_abort`でfail-loud。常駐（`LifeOracle_X_Resident`）を全修正コードで再起動済。X＝note funnel（単発長文ティーザー＋note リンク／最大目的＝noteを開かせる）。委員会はスレッド型を再提案しない。詳細＝`docs/X_POSTING_CHARTER.md`／[[memory: project_x_posting_url_fix]]。
- 🔴 **Anthropic API 月次利用上限到達（2026-06-22）＝API直叩き系（委員会含む）は2026-07-01まで実質停止**。委員会のResearch/Meeting空転はこれが原因（事故でなくコスト上限作動）。X poster はGemini生成（無料）で影響小・Anthropicはフォールバックのみ。復旧＝7/1自動 or Console上限引上げ（俊雄さん判断）。詳細＝[[memory: project_anthropic_api_cost_governance]]。
- 🔴🔴 **【訂正・続報】7/1の自動復旧は実際に機能したが直後に別の障害（プリペイド残高枯渇）で7/2〜7/8現在まで7日連続停止中（as-of 2026-07-08・俊雄さん「委員会が1ヶ月働いていない」で発覚しGitHub Actions実ログで確認）**：`gh run list`実測で、6/22-6/30は月次上限（エラー文面「will regain access on 2026-07-01」）で失敗→**7/1は実際に3回成功（4-5分の実処理）＝月次リセットは予告通り機能した**→**7/2から今日まで7日連続失敗**、エラー文面が別物：`"Your credit balance is too low...Please go to Plans & Billing"`＝**月次上限(自動回復)ではなくプリペイド残高そのものの枯渇(自動回復しない)**。3PJ（Stack Letter/ライフオラクル/サブスクやめた）共有・自動チャージOFFの構造上、2026-06-10に続き2回目の枯渇。**見た目の罠**：`Committee 14:00 Meeting`はresearch失敗時「本日の担当レポートが見つかりません」を正常終了として扱うため、この7日間**毎日GitHub Actions上は緑(success)表示**だった＝空振り成功。∴ **委員会が今動いていないのは「7/1待てば直る」問題ではなく、俊雄さんがAnthropic Consoleでプリペイド残高を手動チャージするまで直らない**。Claudeは決済操作をしない。詳細＝[[memory: project_anthropic_api_cost_governance]]。

### X投稿11日間停止＝環境事故（Playwrightブラウザ消失）を復旧（as-of 2026-07-07）
- 🔧 **X自動投稿が2026-06-26〜07-07の11日間ゼロだった＝コードでなく環境の事故・復旧済み**。真因＝共有 `ms-playwright` フォルダから playwright 1.59.0 が要求する chromium 1217 バイナリが消失（別環境の `playwright install` がGCした可能性大・6/27 22:29起点）。生成は毎回成功しており、6/26 の単発化・280字整合の修正は有効だった。復旧＝`playwright install chromium`（Python314）＋常駐再起動＋**手動テスト実投稿成功を実物確認**（7/7 07:38・型A・note URL付き・画像付き・status/2074261806143689181）。あわせて投稿ID抽出が固定ポスト（6/25設定）のIDを毎回誤記録するバグも修正（x_posting_system commit 090cdf7）。∴ **委員会は「X投稿が止まっている」を6/26修正の失敗と誤認しない（修正は有効・止めたのは環境）**。監視の穴（無投稿N日でSlack通知が無い）は俊雄さんに提案中・未採択。詳細＝`tasks/lessons.md` 2026-07-07節／[[memory: project_x_posting_url_fix]]。

### アプリ実機テストで発覚した6件を修正・デプロイ済み（as-of 2026-07-08）
- 🔧 **診断結果の表示名バグを修正（最重要）**：v1由来の`cognitiveFunctionMap.shadowName`（MBTIの対抗機能ペア基準）と、v2の処方箋2,016件・メカニズムJSON・タイプ選択画面が使う`JUNG_LABEL`（同一機能の光/影表現基準）が16タイプ中6タイプ（ENTJ/ENFJ/ESFP/ISFJ/INTP/INFP）でズレており、**表示名と処方箋の中身が一致しない事故**があった（例：INFP選択で表示は「八方美人」だが処方箋は「頑固者」向け）。処方箋・メカニズムJSON自体は元から正しく、表示層だけが誤っていた。`cognitiveFunctions.js`にJUNG_LABEL/MBTI_TO_JUNGを追記し`MbtiResult.jsx`のtypeName算出・光影表示を統一（commit ddcd6ee）。16タイプ全数をNode機械検証＋ブラウザ実操作（正反対ペアINFP/ENFJ）で確認、独立アドバーサリアルレビューでも0件（再現なし）。∴ **委員会は「診断結果の表示ロジックにバグがある」を過去の課題として扱わない（修正・検証済み）**。
- 🟢 **メール収集フォーム・ChatGPT起動ボタンを撤去**：診断利用者がまだ少ない段階でメール登録を求めると警戒感を与えるため、MbtiResult/AttackResult/Result 3画面全てから`EmailCaptureForm`を撤去（commit 63d079b・コンポーネント本体とlib/leads.js/supabase.jsは将来再開に備え温存）。「ChatGPTで開く」ボタンはChatGPTのログイン画面に飛ぶだけで実用性が無いため撤去、「プロンプトをコピー」のみ残す（commit 509a602）。
- 🟢 **note誘導の推奨先を「対人攻略シリーズ（唯一の有料）」から「行動経済学バイアス解説（無料・多数）」へ切替**：`NotePromo.jsx`（4画面で共有）が唯一の有料シリーズを推していたため、biasInfoからライブ算出する評言（現在8本公開中）へ書き換え（commit 62cb7a3・509a602）。月額980円メンバーシップCTAも停止（`membership`フェーズ定義自体は将来再開用に温存していたが、最終的に完全撤去しCSSも清掃・commit c4391db）。既存の`NoteIndex.jsx`（Entry画面の「note記事まとめ」）が既にバイアス8本の個別リンク付き目次を実装済みと判明＝俊雄さんの「バイアス集の目次案内」要望は概ね実現済み。結果画面から直接NoteIndexを開く導線追加は5ファイル規模（要stress-test）のため今回は見送り・将来提案として保留。
- 🟢 **「今日ひとつだけ試すなら」をタップ不要のインライン表示に変更**：HubCardボタン＋全画面遷移を廃止し「明日の一手」直後にインライン表示（commit 4860ac6）。処方箋の有無で内容が重複するケース（処方箋なし時）は本文の重複行を抑制し、シャドウタイプのみのミクロアクションは常に表示。ブラウザで3パターン（処方箋あり/なし・光/影タイプ）を実操作確認済み。
- 詳細＝`life-oracle-v2/tasks/handoff_app_fixes_2026-07-08.md`（Fable5作成の元指示書）・本コミット群（ddcd6ee〜c4391db）。

### ガチ人クロス送客フッターが一度も発火していない（as-of 2026-07-14・実記事＋schedule.json確認）
- 🔴 **`cross_promo_footer.py`（サブスクやめたへの送客リンク・commit bf04832・2026-06-28マージ）は`gachijin_orchestrator.py`に正しく実装・結線されているが、実際に公開された記事には一度も出力されていない。** 実物確認：本日7/14 07:00公開「見た目で決めガチな人の現場」を直接読了→本文末尾は`life-oracle.jp`診断リンクのみ、サブスクやめたへのリンクは無し。
- **根本原因＝タイミング問題（コードのバグではない）**：`scripts/gachijin_schedule.json`の全45テーマの`pipeline_run_date`は2026-05-06〜2026-06-19の範囲＝**全件がbf04832（6/28）より前にビルド・note側へ予約投稿済み**。note.comの予約投稿は予約時点の本文を保持するだけで公開時にコードを再実行しないため、**11月2日分までの予約済みバックログ全体が、フッター無しの本文のまま公開され続ける**。次にorchestratorが新規ビルドを走らせるまで（＝この45テーマのバックログを使い切るまで、想定11月頃）、この機能は実質死んだコードだった。
- ∴ **委員会は「クロス送客フッターは実装済み・稼働中」という前提で判断しない**。稼働させるには、既存の予約済み記事へ本文PUT更新（サブスクやめた側`unpay_yoru.py`と同型パターン）で遡及修正するか、次のバックログ枯渇まで待つかの二択。**遡及PUTは「既存記事の一括書き換え」に該当し実行前にproposal-stress-test必須**（CLAUDE.md該当セクション）。まだ実行していない・提案段階。詳細＝サブスクやめたプロジェクト側セッションで発見・[[memory: project_cross_promo_note_footer]]要更新。

### アプリ本体への閲覧者が9日間連続ゼロ（as-of 2026-07-20・GA4/GSC/技術面 総合調査で確定）

- 🔴 **2026-07-12〜07-20（9日間）、GA4上でlife-oracle.jpの全チャネル合計セッション・イベント・quiz_start/completeが文字通りゼロ。過去180日（計測開始2026-04-24以降）で最長のゼロ連続記録（それ以前の最長は5日間・05-25〜05-29）。** 5つの独立調査（GA4長期トレンド・GA4ファネル・GSC・配信経路note/X・技術ヘルスチェック）を並列実施し、うち3担当（GA4長期トレンド・GA4ファネル・配信経路）が異なるクエリ手法（週次集計／絶対日付レンジ／source-medium別）で独立に同一結論へ到達＝信頼度高い。最後にセッションが記録されたのは07-11（セッション2・app_loaded 3件）。直前の07-05〜07-11週はquiz_start14〜15・complete10〜11と直近では良好な水準だった＝「じわじわ減った」のではなく「07-11で急に途切れた」形。
- ✅ **「アプリが技術的に壊れて誰も来られない」説は排除済み**：4担当が独立に実機検証（`curl`でHTTP 200確認・実ブラウザで`gtag`/`dataLayer`発火確認・GA4 Realtime APIで即時着地確認・Netlifyデプロイ`ready`かつ最新コミットae82688と一致・診断フローを実際に3ステップ進めてコンソールエラー0件）。アプリコード（life-oracle-v2）は07-11 07:37（ae82688）以降無変更。∴ 委員会は「サイトが落ちている/計測が壊れている」を原因候補として再提案しない。
- ✅ **上流の発信（X投稿）も止まっていない**：`x_posting_system/data/posts_log.jsonl`確認、07-12〜07-20も1日2回（07:00/22:30枠）の通常投稿が継続。note記事の予約公開も途切れず（9〜11月分まで予約済み）。
- 🔧 **原因は「発信より下流・GA4より上流」の未取得区間にある（未確定）**：X投稿のインプレッション/クリック数（posts_log.jsonlには送信記録のみで閲覧数がない）と、note記事側のPV・スキ数の直近推移（GA4/GSCの管轄外）が、5担当とも取得できなかった。統計的な「たまたまの谷」で説明するにはポアソン近似で9日連続ゼロの確率は0.1%未満と低く、単独説明としては弱い。∴ **次の一手はnote.com管理画面のPV推移確認とX管理画面のインプレッション確認**（詳細＝`tasks/committee/LEDGER.md` 2026-07-20節があれば参照、無ければ本調査が一次情報）。
- 🔴 **【重要・独立の慢性課題】GSC（Google検索経由）は今回の9日間ゼロとは無関係の別問題として存在**：GSC全履歴（2025-04-22〜2026-07-16・約15ヶ月）で合計クリックはわずか**1回**。2026-06-19のNetlify重複URL修正の前後で「0→月1クリック・週15表示」への変化はあったが、実用的な意味での回復とは言えない水準が現在も継続。06-19節の「SEO不振の真因はNetlify重複」は技術的には正しいが、**修正後に検索流入が実際に回復したかは未判定のまま保留されていた＝今回、回復は名目的で実質ゼロのままと確定**。委員会がこの節を「解決済み」と読んで蒸し返さないのは正しいが、「検索流入が改善傾向」と誤読しないよう本節を参照すること。
- 🔧 **副次発見：`daily_report.py::get_gsc_stats()`にバグ**。`dimensions=["query"]`の合計だけを見ているため、Googleの検索クエリ匿名化（表示回数が少ないクエリ文字列は伏せられる仕様）で行が丸ごと欠落し、実際は表示15回あるのに「表示0」と過小report される。`dimensions=[]`の集計値を使うよう修正が必要（未修正・次回セッションで対応候補）。
- ✅ **副次発見：`life-oracle.com`（保護ドメイン）がDNS解決失敗**。`google.com`/`github.com`は正常解決するため`life-oracle.com`固有の問題（レジストラ側の設定切れ等の可能性）。主要導線ではないため緊急度は低いが未調査。
- 🔴 **【追記・note.com公開ページで直接確認】5担当が「未取得」としていたnote記事側の反応も、この9日間ほぼゼロと確認。** `https://note.com/lifeoraclejp` を実際に開いて確認したところ、ゼロ期間中（7/12〜7/20）に投稿された夜のトリセツ・私のトリセツ・jin_系の記事はほぼ全てスキ数の表示自体が無い（＝0件）。数字が付いているのは3日前の記事（1件）と4週間前の固定記事「神プロンプト配布」（6件）のみ。記事本文・CTAリンク（`👉 診断スタート`→`life-oracle.jp?utm_source=note...`）は正しく設置されており構造上の破損はない。**∴「配信（X投稿）は止まっていないのに着地（GA4）がゼロ」という謎の空白は、note記事そのものへの反応（スキ）も同時にゼロになっていることと整合する＝note記事が読まれていない/反応されていない可能性が高い。** ただし公開ページはスキ数のみで閲覧数(PV)は著者本人のみ閲覧可能なnote管理画面（ダッシュボード）でしか確認できず、「読まれた上で反応しなかった」のか「そもそも読まれていない」のかはこれだけでは切り分け不可（俊雄さんのnoteログインでの確認が必要）。

### Netlifyアクセスログ実測＋note.comダッシュボードPV実測で「反応が無さ過ぎる」を確定（as-of 2026-07-20・両方とも管理画面ログインで直接確認）

- 🔴 **Netlifyの生HTTPアクセスログ（Observability機能・GA4を介さないサーバー側の実測）で確認：直近7日間（07-13〜07-20）のtext/htmlリクエスト504件のうち、実際の診断入口`/mbti/entry`へのアクセスはたった1件（しかもUser-Agent分類は「Tools」＝自動化ツール系で人間と断定できない）。** root`/`は119件あるが、User-Agent分類「Browser」142件の内訳を直接確認したところ上位は`/wp-includes/js/jquery/jquery.js`・`/media/system/js/core.js`（Joomla探索）・`/app`/`/settings`/`/console`/`/dashboard`/`/login`/`/admin`（同一ミリ秒に連射＝botの一斉探索）等の脆弱性スキャナー特有のパスで占められていた。トップ被アクセスパスは`/wp-admin/install.php`（102件）・`/wp-login.php`（11件）・`/xmlrpc.php`（11件・エラー率100%）・`/.env`（9件）・`/.git/config`（6件）と、本アプリがReact/Viteである以上すべて無関係な自動巡回スキャナーの痕跡。
- ✅ **これにより07-20時点の結論を更新：「GA4計測が壊れていて実際は人が来ている」という仮説（前節の伏線）は棄却する。** サーバー生ログ側でも実質的に人間の診断利用が確認できない＝GA4が9日間ゼロを報告していたのは計測の不具合ではなく、実態に近い可能性が高い。
- 🔴 **note.com公式ダッシュボード（`https://note.com/sitesettings/stats`・俊雄さんアカウントで直接ログイン確認）の週次PV推移が、約1ヶ月かけて継続的に下降している：** 06/16-06/22＝355ビュー・06/23-06/29＝339・06/30-07/06＝232・07/07-07/13＝140・07/14-07/20＝107。月次比較でも過去30日（06/21-07/20）＝924ビュー・55スキ に対し、その前の30日（05/21-06/20）＝2,661ビュー・183スキ と**約65%減**。全期間累計＝6,247ビュー・637スキ（記事686本）。
- 🔧 **下降開始のタイミングが06/27〜07/06のX投稿11日間停止（本台帳「X投稿11日間停止」節・07-07に復旧確認済み）と重なる**が、X投稿復旧後（07/07以降）もビュー数の下降は止まらず07/07-07/13＝140→07/14-07/20＝107とさらに減り続けている。∴ **X停止は下降のきっかけの一つとしては整合するが、それだけでは07/07以降の継続的な下降を説明しきれない**（noteアルゴリズム側の露出減衰・読者離れの複合要因の可能性が高いが未特定）。
- ✅ **「note記事の反応が無さ過ぎるのではないか」という俊雄さんの指摘は、公開ページのスキ数だけでなくnote公式ダッシュボードのPV実数でも裏付けられた＝確定事実。** 「読まれた上で反応しなかった」のか「そもそも読まれていない」のかの切り分け（前節の残課題）は、今回のPV実測により後者（そもそも読まれていない＝ビュー自体が減少）が濃厚と判明。次の一手＝note側のこの4週間の下降要因の特定（アルゴリズム露出・投稿頻度・タイトル傾向等）と、Netlify側の脆弱性スキャナー流入（実害は今のところ無いが継続監視の価値はある）。

### 配信実験の開始＋量産の段階縮小（as-of 2026-07-21・俊雄さん承認・proposal-stress-test済）

- 🟢 **9週間の配信実験を開始（2026-07-21〜09-21）。** 前節までの調査（アプリ実訪問ほぼゼロ・note週次ビュー5週連続下降）を受け、「配信エンジン不在が主因」の仮説を初めて能動的にテストする。設計＝`WEEKLY_SPRINT.md`配信実験節。**基準線（2026-07-21実測）：noteフォロワー141（公開API）・Xフォロワー53（07-12時点）・note週次ビュー107（07/14-20）・1記事あたり平均スキ約0.4・全期間6,247ビュー/637スキ。** 主ゲート＝noteフォロワー161以上、判定＝9/21(月)の週次委員会。未達時は別の配信手法1つ or 休眠検討（コンセプト見直しはしない）。無効条項＝俊雄さんの読む日課の実施率が半分未満の週が続いたら判定延期。
- ✅ **jin_有料（500円）は現状維持と決定＝委員会は「jin_無料化」を提案しない。** 2026-07-21実測：有料jin公開25本の平均スキ0.40 vs 無料バイアス記事9本の平均0.44＝**有意差なし**。2026-05-13のyoru_実験（有料化で14倍減）とは異なり、今回は価格が主因である証拠がない。配信問題の解決前に価格を動かすのは「動くために動く」だけと判断（stress-test内で確認）。なおjin_18〜48は公開APIで404＝**予約段階の未公開でありデータ消失ではない**（listAPIは予約を隠す既知仕様）。
- 🟢 **新規バルク生成は停止（post_季節枠 月2本のみ例外・拡大禁止）。** 生成系スケジュールタスクは全てDisabled済みを2026-07-21に実機確認（Pipeline_0430/1230/1830・Codex_Prep/Pickup）。稼働中はX告知系・監視系・週次委員会のみ＝生成停止に追加のタスク操作は不要。手動バルク生成を行わないという運用決定。
- 🔧 **予約済みバックログの実態（2026-07-21棚卸し・pipeline/logs＋posted/実ファイルで確認）**：今日以降の予約はyoru_41+post_64+jin_31=136本＋gachi_（別リポ管理・〜11月上旬まで毎日07:00）。**投稿密度は本日から自然に約3.3→2.3本/日へ低下する**（post_は07/21〜8月下旬まで予約が無く自然休止・yoru_は8月末No118まで毎日）。渋滞月は**9月のみ（約100本・3.3本/日）**、11月以降はgachi_のみ約1本/日→ゼロ。∴ Stage1（〜8月末）は**予約に一切手を触れず**自然低下を利用して観測に徹する。Stage2＝8月末に、9月分の汎用記事（実日付紐づき6本を除く）を空いている11月以降へ再日付するかを実験データを見て判断（＝元々の「11月から1日1本」構想の実現形）。**再日付の一括実行はその時点で再確認＋PUTのpublish_at null化バグ（2026-06-08 lessons既知）への対策必須。**
- ⚠️ 注意：ローカルpipeline/logsには07/21〜31のyoru_No77〜87と8月中旬〜末のyoru_No100〜118の予約記録が欠落しているが、**posted/フォルダに実ファイルが存在＝予約済み**（ログの欠落であって予約の欠落ではない）。棚卸しをログだけで行うと7-8月が空白に見える罠がある。

---
*更新：解決・設定が確定した事項を as-of 付で追記。古くなった/矛盾が出た項目は日付を更新するか削除する。*
