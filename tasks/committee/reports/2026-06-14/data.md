# 📊 データ担当 日次レポート — 2026-06-14

## 調査サマリ（今日わかったこと・3〜6点、箇条書き）

- セッション4 / ユーザー3 / PV3 という極小トラフィック。**統計的判断はまだ不可能**で、現状は「アクセスがほぼ無い」ことが最大の事実。
- 平均滞在 **3分44秒** は、この規模としては長い。少数だが「触った人はそれなりに滞在している」=コンテンツ自体は時間を奪えている可能性。
- 直帰率 **75%**（4セッション中3が直帰）。ただし母数4なので「3/4が即離脱」という生の挙動として読むべき。
- PV3 に対しセッション4 = 1セッションあたり1ページ未満の遷移。**診断ファネルの中間ページ遷移がPVとして取れていない疑い**（SPA内遷移がPVに計上されない設計の可能性）。
- 完走率・離脱設問・結果タイプ分布は **本日も算出不能**。基本指標は取れているが、診断ファネルのカスタムイベント（diagnosis_start / question_view / diagnosis_complete）が未実装のため。

## なぜ重要か（理由）

- 滞在3分44秒は「凄いと言いたくなる体験」の **必要条件（時間を奪える）は満たしつつある** サイン。中身が薄ければ秒で閉じられる。ここは希望の数字。
- 一方で直帰率75%は、滞在した一部と「来てすぐ帰る大多数」が混在している可能性を示す。**入口（LP/最初の設問）で振り落としている**なら、せっかくの滞在価値に到達する前に失っている。
- PVとセッションの不整合は、**そもそも診断の進行をGA4が観測できていない**ことを強く示唆。これが解決しないと、滞在3分44秒が「設問を進めた結果」なのか「最初のページで読み耽った結果」なのか区別できず、改善の打ち所が定まらない。
- 噂・自然言及（北極星）に至るには「完走 → 結果に感動 → シェア」の連鎖が必要。今は **完走したかどうかすら測れていない**ため、北極星までの距離が数値化できていない。

## 改善提案（具体・実装可能・優先度つき）

1. **【最優先】診断ファネルのカスタムイベント実装（エンジン／UX担当へ）**
   - `diagnosis_start`（開始）／`question_view`（設問IDをparamに）／`diagnosis_complete`（結果タイプIDをparamに）の3点を最低限実装。
   - これが入れば「3分44秒が何の3分44秒か」が翌日から分解できる。SPA内遷移を `page_view` か独自イベントで送る設定もここで同時に。

2. **【高】流入をまず作る（SEO／コンテンツ担当へ）**
   - 母数3ユーザーでは何を改善しても検証できない。最低でも日次2桁ユーザーに乗せる施策（俊雄さんX/noteからの動線強化、結果ページのOGP最適化）を先行。
   - 分析担当として明言：**今のデータで結論を出すこと自体が誤り**。まず観測可能な母数を作るのが先。

3. **【中】入口1画面目の離脱仮説検証（UX担当へ）**
   - 直帰3/4の生挙動を、イベント実装後に「diagnosis_startすら踏まなかった人」と切り分ける。LPで「何が始まるか」が伝わっているか要確認。

4. **【低】滞在3分44秒の中身を後追い**
   - イベント実装後、滞在時間を「設問回答中」と「結果閲覧中」に分解。結果ページ滞在が長ければシェア動線をそこに置く根拠になる。

## 確信度と次の一手（高/中/低 と、明日深掘りすること）

- **確信度：低**（母数4セッションのため、傾向の断定はできない。「滞在は長め」「観測の穴がある」という構造的指摘のみ確信度は中）。
- **明日深掘りすること：**
  1. カスタムイベント実装の進捗確認（実装されれば翌日から完走率の初データが出る）。
  2. PV<セッションの原因が「SPA内遷移未計測」か「実際に1ページで帰っているか」の切り分け。
  3. 流入施策の有無を各担当に確認 ── **数字を語る前に、語れるだけの母数を作る合意形成**を最優先課題として提起する。
