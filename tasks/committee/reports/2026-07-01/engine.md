# ⚙️ エンジン担当 日次レポート — 2026-07-01

## 調査サマリ（今日わかったこと・3〜6点）

- 直近10実行のうち、実体としての失敗（`failure`）は0件。ヘッダーの「失敗6件」は、`in_progress`（計3件）を失敗としてカウントした集計ノイズと推測される。
- `Committee 10:00 Research` が3回連続で `in_progress` のまま滞留している。実行がハングしている、または長時間ジョブが完了せず次回にまたがっている可能性がある。
- `Daily Monitor Report`（4回）と `Committee 14:00 Meeting`（3回）は全て `success` で安定。デプロイ・ビルド系の回帰兆候は本日のCIシグナルからは検出されず。
- 引退済みワークフロー（gachijin 等）の失敗は今回のログに混入しておらず、対象外フィルタは正しく効いている。
- 診断ロジック（App.jsx側の処方箋マッチング）の回帰を示すシグナルは、今日のCIログには現れていない。

## なぜ重要か（理由）

- **`in_progress` 滞留は「静かな詰まり」**：失敗として赤く出ないため見逃されやすいが、Researchジョブがハングしていると翌日以降の集計・レポートに古いデータが混ざり、委員会全体の判断精度を静かに劣化させる。北極星（自然発生的な言及）に向けた改善サイクルの回転数が落ちる。
- **集計ノイズ（失敗6件表示）は意思決定を歪める**：本当は健全なのに「失敗多発」と誤認すると、無駄な調査コストが発生し、本来やるべき体感速度改善に時間が割けなくなる。
- **success安定の確認は攻めの余地を示す**：CIが健全なら、リスクを取ってUX（初動の軽さ）改善に踏み込める根拠になる。

## 改善提案（具体・優先度つき）

**優先度 高**
1. `Committee 10:00 Research` に**タイムアウトを明示設定**する。ワークフローの該当ジョブに `timeout-minutes: 15`（実測に応じ調整）を追加し、ハング時に自動的に `failure` として顕在化させる。今は詰まりが `in_progress` に隠れている。
2. 3回連続 `in_progress` の実行ログを1件だけ開き、**どのステップで止まっているか**（ネットワーク待ち／APIレート制限／外部フェッチのリトライ待ち）を特定する。ステップ名まで割り出して明日報告する。

**優先度 中**
3. 監視ヘッダーの集計式を修正：`in_progress` を「失敗」に含めず、`running`（進行中）として別カウントする。conclusion が null のものは失敗扱いしない、という一行の条件分岐でノイズが消える。

**優先度 低**
4. Research系ジョブが外部フェッチに依存するなら、`stress_test.mjs` で **外部呼び出しの応答時間の分布（p50/p95）** を測り、タイムアウト値の根拠を数値化する。体感より先に「何秒で詰まるか」を確定させる。

## 確信度と次の一手

- **確信度：中**。「実失敗0・success安定」は確信度高。ただし「in_progress滞留＝ハング」か「単に長いだけ」かは、ログのステップまで見ないと断定できない（ここは中）。
- **明日深掘りすること**：`Committee 10:00 Research` の in_progress 実行を1件開き、停止ステップと経過時間を特定。そのうえで妥当な `timeout-minutes` を提案する。あわせて監視集計の `in_progress` 除外を適用済みか確認する。
