# ⚙️ エンジン担当 日次レポート — 2026-06-09

## 調査サマリ（今日わかったこと・3〜6点）

- 直近10実行のうち9件が success、1件（Committee 10:00 Research）が in_progress で、明確な失敗はゼロ。CIシグナル上は安定している。
- ただし「失敗 16 件」という累積カウントが付帯しており、過去スパンで一定数の失敗が蓄積していることを示唆。今日の10件には含まれないため、古い不安定要因の残滓と推定。
- Daily Monitor Report / Committee 14:00 / 10:00 Research の3系統が規則的に回っており、スケジュールジョブの定期実行は健全。
- 失敗が「直近10件には出ていない」＝ここ最近の修正・運用変更で安定化した可能性。回帰（デグレ）の即時兆候は見えない。
- in_progress の Research ジョブが長時間滞留していないかは、実行時間ログを見ないと判断できない（本素材では時刻のみで継続時間が不明）。

## なぜ重要か（理由）

- CIが緑であること自体は「初動の軽さ」を直接は保証しないが、デプロイが詰まらない＝ユーザーに最新版が届く速度が保たれることを意味する。噂が立つ瞬間に最新の体験を出せる土台。
- 累積16失敗を放置すると、何が「正常な失敗」で何が「危険な失敗」かの感覚が鈍り、本当のデグレを見逃す。失敗の中身を分類しないと、緑/赤の信号が信頼できなくなる。
- in_progress の滞留は、スケジュールジョブの重複起動やAPIタイムアウトの前兆になりやすい。Researchジョブは外部呼び出しを含む可能性が高く、体感とは無関係でもCIの安定性を蝕む。
- 定期ジョブが正常稼働＝委員会の運用パイプライン自体が壊れていない、という安心材料。ここが崩れると日々の改善サイクルが止まる。

## 改善提案（具体・優先度つき）

**優先度：高**
1. 累積16失敗の「中身分類」を1回だけ実施。GitHub Actions の失敗ログを `タイムアウト / 外部API失敗 / ビルドエラー / フレーク（再実行で成功）` の4ラベルに手作業で振り分け、どれが多いかを把握する。原因の8割は1〜2カテゴリに偏るはず。
2. in_progress の Research ジョブに **timeout-minutes** をワークフローへ明記（例：10分）。滞留して枠を専有する事故を機械的に防ぐ。既存値があれば妥当性を確認するだけでよい。

**優先度：中**
3. `life-oracle-v2/scripts/stress_test.mjs` で、まず計測対象を1つに絞る：**初回表示までの App.jsx 初期化時間（診断ロジックのロード〜最初の入力可能状態）**。体感の「初動の軽さ」に最も効くため。処方箋2016件のデータ読み込みが同期的にブロックしていないかを数値で確認（データ自体は変更しない、読み込みタイミングのみ計測）。
4. Daily Monitor Report の出力に「直近10件の成功率」を1行追加し、緑の連続日数を可視化。回帰の兆しを日次で早期検知できる。

**優先度：低**
5. フレーク（再実行で成功する不安定ジョブ）が分類で見つかった場合のみ、リトライ1回の自動再実行を設定。本質修正ではないが、緑信号の信頼度を上げる。

## 確信度と次の一手

- **確信度：中**。「直近は安定」は高確信だが、累積16失敗の中身が不明なため、回帰の不在を断言はできない。
- **明日深掘りすること**：(1) 累積失敗16件のログを開いて4分類を完了させる、(2) in_progress だった Research ジョブの実行時間を確認し滞留有無を判定、(3) stress_test.mjs で初回表示時間を1回ベースライン計測し、今日の数字として記録する。
