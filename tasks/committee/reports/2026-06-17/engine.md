# ⚙️ エンジン担当 日次レポート — 2026-06-17

## 調査サマリ（今日わかったこと・3〜6点、箇条書き）

- 直近10実行で**失敗0件**。Committee系（Research / Meeting）とDaily Monitor Reportはいずれも安定して `success`。CI上の回帰シグナルは検知されず。
- 引退済みワークフロー（gachijin 等）の失敗ノイズが**一切混入していない**＝監視対象の絞り込みが正しく効いている。
- 「Committee 10:00 Research」が現在 `in_progress`。これは本日分のジョブが進行中なだけで、過去9実行が全て成功しているため**異常兆候ではない**と判断。
- これらはすべて運用・自動化系のワークフローであり、**App本体（src/App.jsx）のビルド・表示速度に直接関与するジョブは今回のシグナルに含まれていない**。つまりCI緑＝体感速度の保証ではない、という構造を再確認。
- 速度・ビルド時間の実測が `stress_test.mjs` に外出しされている＝CIだけ見ても「初動の軽さ」の回帰は捕まえられない死角がある。

## なぜ重要か（理由）

- CIが安定している＝コミッティ自動化基盤が壊れていない、という土台の健全性を示す。土台が揺れると毎日の所見そのものが出せなくなるので、緑であること自体に価値がある。
- ただし**CI緑と体感速度は別物**。今回の成功ジョブは運用系であり、ユーザーが触る `App.jsx` のレンダリング遅延・初回ロード重量は別計測でしか見えない。ここを混同すると「問題なし」と誤判定し、UX劣化を見逃す。
- 北極星（自然発生的な口コミ）に効くのは「初動の軽さ」と「引っかからない表示」。診断結果が出るまでのもたつきは、人に薦めたくなる瞬間を確実に削る。CIが見ていない領域こそ口コミの生死を分ける。

## 改善提案（具体・実装可能・優先度つき）

**優先度：高 — stress_test.mjs で「初動」を数値化する**
- 計測項目を明示指定：
  1. 開発ビルド時間（`vite build` の total time）
  2. 生成バンドルサイズ（特に初回ロードに乗る main chunk の KB）
  3. 診断1回分の処理時間（入力→結果表示まで、処方箋2016件の照合を含む経路の所要ms）
- これを毎日同じ条件で取り、**前日比の差分**だけを所見に出す運用に。絶対値より「昨日より重くなったか」が回帰検知に直結する。

**優先度：中 — App.jsx のレンダリングを軽い手当てで監視**
- 結果表示部で大きな配列（処方箋・偉人）を毎レンダリングで走査していないか確認。データ本体は触らず、**参照・絞り込みロジックのメモ化（useMemo）有無だけ**点検。重い再計算が走っていればここが体感のもたつき源。

**優先度：低 — CIに軽量な「ビルド成功＋サイズ閾値」チェックを足す検討**
- 既存ワークフローは増やさず、将来的に main chunk が一定KBを超えたら警告する1行チェックを stress_test 結果に連動させる案。今日は提案メモのみ、実装は計測データが溜まってから。

## 確信度と次の一手（高/中/低）

- **確信度：高** — 「CIに回帰シグナルなし」は実データ通りで確実。
- **確信度：中** — 「体感速度の死角がCIに存在する」は構造上の推論。stress_test の実測で裏付ける必要あり。
- **明日の深掘り**：stress_test.mjs を上記3項目（ビルド時間・main chunkサイズ・診断1回の所要ms）で実行し、ベースライン数値を確定させる。基準が取れれば、以降は「前日比だけ報告」で回帰を最速で捕まえられる体制に移行する。
