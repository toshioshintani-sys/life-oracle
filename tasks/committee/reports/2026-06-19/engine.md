# ⚙️ エンジン担当 日次レポート — 2026-06-19

## 調査サマリ（今日わかったこと・3〜6点）

- 直近10実行で**失敗0件**。Daily Monitor / Committee 14:00 / Jin Article Check すべて success で、CI シグナル上は安定運用が継続している。
- `Committee 10:00 Research` が現在 `in_progress`。これは本レポート生成と同じ時間帯のジョブで、未完了は異常ではなく走行中のスナップショットと判断。
- 引退済みワークフロー（gachijin 等）由来の失敗ノイズが**一切混入していない**。RESOLVED_FACTS / NOT_DOING の除外フィルタが効いている状態。
- ただし CI が緑であること＝**体感速度が良いことの保証にはならない**。今日の素材には実行「結果」しかなく、ジョブの**所要時間（duration）の推移**が見えていない。回帰の兆候は成否ではなく所要時間の漸増に出る。
- スケジュール系（Daily Monitor / Committee）が毎日定刻で回っており、ビルド・デプロイ系の異常終了は観測されない。フロント（App.jsx / Vite）側の構成変更による回帰は今日時点では兆候なし。

## なぜ重要か（理由）

- **失敗0は良いが情報量が少ない**：成否は二値で、初動の重さやバンドル肥大はここに出ない。「自然発生的に噂される」北極星を狙うなら、UXの引っかかりこそ監視対象で、それは duration とバンドルサイズの時系列でしか見えない。
- **in_progress を異常と誤読しない**ことは、毎日の判断ブレを防ぐ。走行中ジョブを失敗扱いすると無駄な調査コストが発生し、本来見るべき体感速度から目が逸れる。
- **除外フィルタが効いている**＝過去の引退ワークフローを蒸し返さない運用が確立。これにより監視対象が本当に生きている経路（フロント＋デプロイ）に絞れている。
- 緑が続くときほど**サイレントな漸増（じわじわビルドが重くなる）**を見逃しやすい。北極星はキレのある初動で決まるため、ここを定量化しないと「凄い」体験は守れない。

## 改善提案（具体・優先度つき）

**P1: CI に duration トレンドを足す（最小実装）**
- 各ワークフロー実行の所要時間を1行で記録（`gh run list --json databaseId,name,conclusion,createdAt,updatedAt` から差分秒数を算出）し、Daily Monitor Report に直近7日分の中央値・最大値を追記する。成否だけでなく**「Committee 14:00 が前週比+30%」**のような兆候を拾える。

**P2: stress_test.mjs で初動を測る**
- 計測項目を明確化：①Viteビルド時間、②`dist` 総サイズと最大チャンクサイズ、③App.jsx 初回マウントまでの想定（初期JSパース量の代理指標としてエントリチャンクのgzipサイズ）。
- 北極星の「初動の軽さ」に直結するのは**最大チャンクのgzipサイズ**。これがしきい値（例: 250KB gzip）を超えたら警告を出す。

**P3: 回帰検知のベースライン固定（低コスト）**
- 今日の緑な状態を「基準スナップショット」として duration とバンドルサイズを記録。以降は基準比±20%超で要調査フラグ。大規模リファクタはせず、まず数字の物差しを置くだけ。

## 確信度と次の一手

- **確信度：中**。CIが安定している点は高確信だが、体感速度については今日の素材に時間データが無く推論に留まる。
- **明日深掘り**：①Daily Monitor に duration 集計を1行追加して時系列の地盤を作る、②stress_test.mjs を一度走らせてエントリチャンクgzipサイズの現在値を取得し、P3のベースラインに固定する。まず計測、次に最小修正の順を守る。
