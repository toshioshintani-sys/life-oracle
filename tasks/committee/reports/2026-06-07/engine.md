# ⚙️ エンジン担当 日次レポート — 2026-06-07

## 調査サマリ（今日わかったこと・3〜6点）

- 直近10実行のうち **Gachijin Pipeline が3連続 failure**（success 1回のあと連続で崩れている）。これが今日の最大の異常シグナル。
- 累積失敗 23件という規模感に対し、直近では Gachijin に偏って失敗が集中している。横断的な障害ではなく**特定ジョブ局所の回帰**の可能性が高い。
- Committee（Research/Meeting）と Daily Monitor Report は全て success。**スケジュール系・レポート系は健全**で、フロント本体（App.jsx）に直結する経路の崩れは現時点で観測されない。
- 「Committee 10:00 Research」が in_progress と success の両方に出ている＝同一枠で再実行が走った形跡。**リトライまたは多重起動**の疑い。
- 速度・体感の実測値はこの素材内には無く、CI シグナルからの推論にとどまる（stress_test.mjs 待ち）。

## なぜ重要か（理由）

- Gachijin Pipeline の連続失敗は、データ生成・更新の供給が止まっているサインになり得る。フロントが参照する成果物が古いまま固定されると、**ユーザーから見た「更新の鮮度」=信頼**に直結する。北極星（自然発生的な口コミ）は「いつ見ても生きている感」が支える。
- 失敗が1ジョブに偏るのは、裏を返せば**切り分けが容易＝早く直せる**好機。横断障害でない限り、フロントの体感速度・安定性への波及は限定的と読める。
- Committee / Daily Monitor が安定していることは、**監視と意思決定の土台が機能している**証拠。ここが落ちていないので、Gachijin の修正にリソースを集中できる。
- 同一枠の二重実行（in_progress + success）は、放置すると**無駄な並走でCI時間とAPIコストを食い、本来計測したい速度シグナルにノイズを乗せる**。

## 改善提案（具体・優先度つき）

- **［優先度：高］Gachijin Pipeline 連続失敗の一次切り分け**
  3連続のログを並べ、失敗ステップが同一か（=決定的）／毎回違うか（=不安定/タイムアウト）を判定。
  - 同一ステップ → 直前のコミット差分を確認し、最小リバートまたはその1ステップだけの修正。
  - 毎回バラつく → 外部API/ネットワーク起因の疑い。該当ステップに `retry`（最大2回・指数バックオフ）とステップ単位のタイムアウト明示を追加。

- **［優先度：高］失敗時にフロントが古い成果物で壊れないか確認**
  Gachijin の出力をフロントが読む経路で、**生成失敗時のフォールバック（前回成功分の保持）**が効いているか1点だけ確認。効いていれば体感影響ゼロ＝慌てず直せる。

- **［優先度：中］Committee 枠の二重起動防止**
  該当ワークフローに `concurrency: { group: committee-1000-research, cancel-in-progress: true }` を付与し、同一枠の並走を1本に収束。

- **［優先度：中］stress_test.mjs での実測指示**
  CI からは体感が読めないため、次の3点を計測する：
  1. 初回ロードの **TTI（操作可能になるまで）** … 「初動の軽さ」の代理指標。
  2. 診断1回あたりの **処理時間の中央値とp95** … 引っかかりはp95に出る。
  3. 連続診断時の **メモリ/レスポンス劣化の有無** … 長時間利用での回帰検知。
  ※ 処方箋2016件・偉人16名・cognitiveFunctionMap・光と影の名前はデータとして触らず、読み込み・参照速度のみ測る。

## 確信度と次の一手

- **確信度：中**。「Gachijin が局所的に連続失敗」「レポート系は健全」は実行結果から確度が高いが、**失敗原因（決定的 or 不安定）とフロント体感への波及は未確認**のため断定は避ける。
- **明日の深掘り**：
  1. Gachijin 失敗3件のログ突合で原因タイプを確定し、最小修正を1本入れる。
  2. stress_test.mjs を回し、TTI と診断p95 のベースライン数値を取得して「CIシグナル↔体感」の翻訳表を作り始める。
  3. Committee 二重起動が再発しているか翌日の実行で確認。
